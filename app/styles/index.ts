/**
 * Barrel export for all style constants.
 * Import from '@/styles' or '../styles' for clean imports.
 */

export { colors } from './colors';
export type { ColorKey } from './colors';

export { fontSizes, fontWeights, lineHeights, textStyles } from './typography';

export { spacing, borderRadius, buttonHeight } from './spacing';
export type { SpacingKey, BorderRadiusKey } from './spacing';

export { commonStyles } from './common';
